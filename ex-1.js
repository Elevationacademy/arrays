// - DO NOT modify/delete *any* starter code (you can remove the comments though)

function Exercise1() {
    /*vv Define `p1` and `p2` here vv*/

    /*^^ Define `p1` and `p2` here ^^*/

    const ex = function (p1, p2) {
        //Write your logic here (between the curly braces of `ex`)
    }

    return { people: [p1, p2], ex }
}

//feel free to ignore this:
var x = Exercise1()
typeof module == 'undefined' ? x.ex(x.people[0], x.people[1]) : module.exports = x